# 新版界面设计方向：连载创作室

这轮确定界面语言，并实现写作主界面、完整作品书架、四步工作流建书、创建作品弹窗、作品资料库和 AI 预览弹窗。

## 设计目标

1. 打开界面就能感到“正在创作连载小说”，而不是通用办公表单。
2. 正文区保持长时间阅读和写作的舒适度，控制区承担视觉情绪。
3. AI 建议始终是候选内容：生成、预览、修改、采纳的状态要清楚。
4. 作品、章节、正文、AI 四个区域的层级一眼可辨。

## 视觉规则

| 用途 | 设计 |
|---|---|
| 顶部与作品栏 | 深紫黑作为舞台背景，营造夜间连载的氛围 |
| 主要操作 | 玫红至珊瑚红渐变，仅用于创建、生成、采纳等关键动作 |
| 辅助点缀 | 暖金色星点和书脊，提示灵感与作品 |
| 正文 | 暖白纸张、宋体、较宽行距，避免深底长篇阅读疲劳 |
| 章节栏 | 浅紫灰底，当前章用柔和粉底和高对比序号突出 |
| AI 面板 | 紫红色标题区，任务选项、对话、输入区分层 |

主要色值：夜色 `#231d31`、玫红 `#ed526d`、暖金 `#ffbd7b`、纸张 `#fffdf9`。正文建议 17px、行高 2.15；窄屏降至 15px。

## 页面结构

```
顶部：品牌与书架入口 / 本机保存状态 / 导入导出 / 模型设置
书架：作品搜索与排序 / 章节、字数、资料概览 / 继续最近写作章节
建书：创作方向 / 故事骨架 / 人物世界 / 确认建书；每次 AI 生成均先预览与采纳
建书记录：多份草稿与完成方案 / 继续编辑 / 复制 / 备份导入导出
左一：作品书架
左二：章节目录
中央：章节路径 / 故事概念与设定 / 纸张正文
右侧：AI 共创 ↔ 作品资料；资料按世界观、人物设定、时间线、剧情规划查看
浮层：资料集中编辑、章节版本预览与恢复、模型设置、生成内容预览和采纳
```

`?ui-preview=1` 是开发环境专用的界面样例，展示一部虚构作品、四章目录和一段 AI 对话，不会读写用户作品。添加 `&panel=shelf` 查看作品书架；添加 `&panel=workflow` 查看建书流程；添加 `&panel=workflow-history` 查看带样例草稿的建书记录；添加 `&panel=reference` 查看作品资料栏；添加 `&panel=preview` 查看“预览并采纳”弹窗。直接打开普通地址进入真实本地数据。

## 交互原则

- 主按钮只用于进入下一步；导入、导出、设定等次级动作使用轻量按钮。
- 空书架提供创建、导入、配置模型三个真实入口；创建作品使用站内弹窗填写书名和一句话灵感。
- 当前作品和章节必须有明确状态；保存状态持续显示。
- AI 结果先停在对话和预览弹窗，采纳前不修改正文或设定。
- 支持键盘焦点高亮与系统“减少动态效果”偏好。

## 待设计的页面

现阶段仍需继续设计 AI 候选并排对比、逐字版本比较、每日写作统计、移动端写作布局。建书流程已有四步草稿、AI 候选预览、章节提纲生成；建书记录已支持多草稿与备份；章节历史已有手动快照、AI 写入前快照与安全恢复；作品书架已可搜索、排序并进入最近写作章节；时间线已有基础条目与故事内时间字段，完整事件关联和图形时间轴留待下一阶段。当前窄屏布局是可用的基础适配，尚未做移动端专门交互。

## 全产品页面规划

下表记录各页面的界面契约。完整书架、建书工作流与记录、作品资料库和章节历史已实现基础功能，其余页面仍在规划中。

| 页面 | 用户首先要看到 | 主要操作 | 与写作页的关系 |
|---|---|---|---|
| 完整书架 | 作品封面、最近写作、进度、分类 | 继续写、创建、导入 | 一步回到最近章节 |
| 建书工作流 | 当前步骤、已确定的创作方向、可修改的结果 | 生成候选、编辑、采用 | 完成后生成作品和章节计划 |
| 作品资料库 | 大纲、人物、世界、时间线、故事线 | 新增、编辑、关联正文 | AI 生成时按相关性引用 |
| 章节历史 | 当前稿与过去版本的差异 | 对比、恢复、复制片段 | 从编辑器进入，返回原章节 |
| 灵感收集 | 一句话想法、标签、来源 | 记录、整理、转入作品 | 可成为建书起点 |
| 写作统计 | 今日目标、连载节奏、手写/AI 来源 | 调整目标、查看趋势 | 不在正文页持续打扰 |
| 模型与提示词 | 当前模型、连接状态、任务模板 | 测试、切换、编辑 | 错误信息能追溯到具体任务 |

优先设计顺序：**写作主界面 → 完整书架 → 建书工作流 → 作品资料库 → 历史版本**。写作页是视觉与交互基准；后续模块复用同一套颜色、间距、弹窗和状态反馈。

## 设计截图

以下是开发环境的样例内容，不包含真实作品。图片保存在项目的 `design` 目录。

- [写作主界面](./design/writing-final.png)
- [工作流建书](./design/workflow-desktop.png)
- [窄屏工作流建书](./design/workflow-mobile.png)
- [建书记录](./design/workflow-history.png)
- [窄屏建书记录](./design/workflow-history-mobile.png)
- [空书架](./design/welcome-final.png)
- [AI 预览并采纳](./design/adopt-preview.png)
- [窄屏写作布局](./design/mobile-preview-500.png)
